# RAG Support Bot

RAG-based customer support bot demo powered by FastAPI, ChromaDB, and OpenAI.

## Setup

```bash
python -m venv venv
source venv/bin/activate
pip install -r requirements.txt
```

## Run

```bash
uvicorn app.main:app --reload
```

## Deploy

Deployed automatically to Railway on merge to main.
